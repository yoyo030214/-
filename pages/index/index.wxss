/**index.wxss**/
page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}
.scrollarea {
  flex: 1;
  overflow-y: hidden;
}

.container {
  padding: 0;
  background: #f5f5f5;
}

.header {
  padding: 20rpx 0;
  text-align: center;
}

.title {
  font-size: 40rpx;
  font-weight: bold;
  color: #333;
  display: block;
}

.subtitle {
  font-size: 28rpx;
  color: #666;
  margin-top: 10rpx;
  display: block;
}

/* 导航栏 */
.nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: #fff;
  padding: 20rpx 30rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.nav-left {
  display: flex;
  align-items: center;
}

.nav-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  margin-left: 20rpx;
}

.nav-right {
  display: flex;
  align-items: center;
}

.nav-icon {
  font-size: 40rpx;
  color: #333;
  margin-left: 30rpx;
}

/* 商品列表 */
.product-list {
  max-height: calc(100vh - 400rpx);
}

.product-card {
  background: #fff;
  border-radius: 10rpx;
  margin-bottom: 20rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.1);
}

.product-info {
  padding: 20rpx;
}

.product-name {
  font-size: 32rpx;
  color: #333;
  display: block;
  margin-bottom: 10rpx;
}

.product-price {
  font-size: 36rpx;
  color: #e4393c;
  font-weight: bold;
}

.buy-btn {
  width: 100%;
  height: 80rpx;
  line-height: 80rpx;
  background: #07c160;
  color: #fff;
  border: none;
  border-radius: 0;
}

/* 开发中功能 */
.developing-section {
  margin-top: 20rpx;
  background: #fff;
  border-radius: 10rpx;
  padding: 20rpx;
}

.section-title {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.section-title text {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.more {
  font-size: 24rpx;
  color: #666;
}

.developing-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.developing-item {
  background: #f8f8f8;
  padding: 30rpx;
  text-align: center;
  border-radius: 8rpx;
  color: #666;
  font-size: 28rpx;
}

.developing-item:active {
  background: #f0f0f0;
}

/* 搜索栏 */
.search-bar {
  margin-top: 100rpx;
  padding: 20rpx 30rpx;
  background: #fff;
  position: relative;
  z-index: 99;
}

.search-box {
  display: flex;
  align-items: center;
  background: #f5f5f5;
  padding: 16rpx 24rpx;
  border-radius: 32rpx;
}

.search-icon {
  font-size: 32rpx;
  color: #999;
  margin-right: 12rpx;
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #333;
}

/* 轮播图 */
.banner {
  height: 300rpx;
  margin-bottom: 20rpx;
}

.banner image {
  width: 100%;
  height: 100%;
}

/* 功能区 */
.feature-section {
  background: #fff;
  padding: 30rpx;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.feature-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
}

.feature-item image {
  width: 80rpx;
  height: 80rpx;
}

.feature-item text {
  font-size: 24rpx;
  color: #333;
  font-weight: 500;
}

.feature-desc {
  font-size: 22rpx;
  color: #666;
  text-align: center;
  line-height: 1.4;
}

/* 本地特产推荐 */
.local-products {
  background: #fff;
  padding: 30rpx;
  margin-bottom: 20rpx;
}

.product-scroll {
  white-space: nowrap;
  padding: 10rpx 0;
}

.product-scroll .product-item {
  display: inline-block;
  width: 280rpx;
  margin-right: 20rpx;
  background: #fff;
  border-radius: 10rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.product-scroll .product-item:last-child {
  margin-right: 0;
}

.product-scroll .product-item image {
  width: 100%;
  height: 280rpx;
}

/* 当季时令推荐 */
.seasonal-products {
  background: #fff;
  padding: 30rpx;
  margin-bottom: 20rpx;
}

.seasonal-products .product-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

/* 农户直供推荐 */
.farmer-products {
  margin: 30rpx 0;
  padding: 0 20rpx;
}

.farmer-list {
  margin-top: 20rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.farmer-item {
  display: flex;
  align-items: center;
  background-color: #fff;
  padding: 20rpx;
  border-radius: 12rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
  position: relative;
  overflow: hidden;
}

.farmer-item::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 6rpx;
  height: 100%;
  background-color: #07c160;
}

.farmer-item image {
  width: 120rpx;
  height: 120rpx;
  border-radius: 60rpx; /* 圆形头像 */
  margin-right: 20rpx;
  object-fit: cover;
  border: 2rpx solid #e0f2e9; /* 添加浅绿色边框 */
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1);
  transition: transform 0.3s ease;
}

.farmer-item image:active {
  transform: scale(1.05);
}

.farmer-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.farmer-name {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 8rpx;
}

.farmer-location {
  font-size: 26rpx;
  color: #07c160;
  display: block;
  margin-bottom: 8rpx;
  display: flex;
  align-items: center;
}

.farmer-location::before {
  content: '📍';
  margin-right: 4rpx;
}

.farmer-desc {
  font-size: 26rpx;
  color: #999;
  display: block;
  line-height: 1.4;
}

/* 农户故事 */
.farmer-stories {
  background: #fff;
  padding: 30rpx;
  margin-bottom: 20rpx;
}

.story-list {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.story-item {
  border-radius: 10rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.story-item image {
  width: 100%;
  height: 300rpx;
  display: block;
}

.story-info {
  padding: 20rpx;
}

.farmer-name {
  font-size: 24rpx;
  color: #666;
  background: #f5f5f5;
  padding: 4rpx 10rpx;
  border-radius: 4rpx;
  display: inline-block;
  margin-bottom: 10rpx;
}

.story-title {
  font-size: 28rpx;
  color: #333;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.4;
}

/* 特色产品 */
.featured-products {
  background: #fff;
  padding: 30rpx;
  margin-bottom: 20rpx;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.product-item {
  background: #fff;
  border-radius: 10rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.product-item image {
  width: 100%;
  height: 300rpx;
}

.product-info {
  padding: 20rpx;
}

.product-name {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 10rpx;
}

.product-meta {
  display: flex;
  gap: 10rpx;
  margin-bottom: 10rpx;
}

.origin, .farmer {
  font-size: 24rpx;
  color: #666;
  background: #f5f5f5;
  padding: 4rpx 10rpx;
  border-radius: 4rpx;
}

.price-section {
  display: flex;
  align-items: baseline;
}

.price {
  font-size: 32rpx;
  color: #ff6b6b;
  font-weight: bold;
}

.unit {
  font-size: 24rpx;
  color: #999;
  margin-left: 4rpx;
}

/* 助农活动 */
.promotion-section {
  background: #fff;
  padding: 30rpx;
}

.promotion-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.promotion-item {
  display: flex;
  gap: 20rpx;
  padding: 20rpx;
  background: #f9f9f9;
  border-radius: 10rpx;
}

.promotion-item image {
  width: 200rpx;
  height: 150rpx;
  border-radius: 8rpx;
}

.promotion-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.promotion-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 10rpx;
}

.promotion-desc {
  font-size: 24rpx;
  color: #666;
  margin-bottom: 10rpx;
}

.promotion-meta {
  display: flex;
  justify-content: space-between;
  font-size: 24rpx;
  color: #999;
}

/* 浮动菜单 */
.float-menu {
  position: fixed;
  right: 30rpx;
  bottom: 100rpx;
  width: 80rpx;
  height: 80rpx;
  background: #07c160;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 10rpx rgba(0, 0, 0, 0.1);
}

.float-menu text {
  color: #fff;
  font-size: 40rpx;
}

/* 更多功能弹出层 */
.more-features {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  visibility: hidden;
  opacity: 0;
  transition: all 0.3s;
}

.more-features.show {
  visibility: visible;
  opacity: 1;
}

.mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
}

.features-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: #fff;
  border-radius: 20rpx 20rpx 0 0;
  padding: 30rpx;
  transform: translateY(100%);
  transition: transform 0.3s;
}

.more-features.show .features-content {
  transform: translateY(0);
}

.features-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30rpx;
}

.features-header text {
  font-size: 32rpx;
  font-weight: bold;
}

.close {
  font-size: 40rpx;
  color: #999;
  padding: 10rpx;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 30rpx;
  padding: 20rpx 0;
}

.features-grid .feature-item {
  text-align: center;
}

.features-grid .feature-item text:first-child {
  font-size: 40rpx;
  color: #07c160;
  margin-bottom: 10rpx;
}

.features-grid .feature-item text:last-child {
  font-size: 24rpx;
  color: #333;
}

/* 天气部分样式 */
.weather-section {
  background: linear-gradient(to right, #07c160, #09b864);
  padding: 20rpx 30rpx;
  margin: 20rpx;
  border-radius: 16rpx;
  color: #fff;
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.2);
}

.weather-info {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.weather-location {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 8rpx;
}

.location-icon {
  width: 28rpx;
  height: 28rpx;
}

.city {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.weather-detail {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.weather-icon {
  width: 60rpx;
  height: 60rpx;
}

.temp {
  font-size: 48rpx;
  font-weight: bold;
}

.desc {
  font-size: 28rpx;
  opacity: 0.9;
}

/* 底部导航栏 */
.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 100rpx;
  background: #fff;
  display: flex;
  align-items: center;
  justify-content: space-around;
  box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
}

.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  height: 100%;
  position: relative;
  transition: all 0.3s;
}

.tab-item .iconfont {
  font-size: 44rpx;
  color: #999;
  margin-bottom: 4rpx;
}

.tab-item text:last-child {
  font-size: 24rpx;
  color: #999;
}

.tab-item.active {
  color: #07c160;
}

.tab-item.active .iconfont,
.tab-item.active text:last-child {
  color: #07c160;
}

/* 图片点击动画 */
.product-item,
.farmer-item,
.story-item {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.product-item:active,
.farmer-item:active,
.story-item:active {
  transform: scale(0.95);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

/* 农户产品数量标签 */
.farmer-products-count {
  position: absolute;
  right: 20rpx;
  top: 20rpx;
  background-color: #07c160;
  color: white;
  font-size: 24rpx;
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  text-align: center;
  border-radius: 50%;
  box-shadow: 0 2rpx 6rpx rgba(7, 193, 96, 0.3);
}

/* 农户标签 */
.farmer-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-bottom: 8rpx;
}

.farmer-tag {
  font-size: 22rpx;
  color: #07c160;
  background-color: rgba(7, 193, 96, 0.1);
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
  display: inline-block;
}
