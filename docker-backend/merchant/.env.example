# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=merchant_db
DB_USER=root
DB_PASS=your_password_here

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# 服务器配置
PORT=9000
NODE_ENV=development

# 跨域配置
CORS_ORIGIN=*

# 文件上传配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880 